import { Component } from '@angular/core';
import { FormBuilder, Validators } from '@angular/forms';

@Component({
    moduleId: module.id,
    selector: 'dog-form',
    styleUrls: ['../stylesheets/style.css'],
        template: `
            <h1>Not currently used</h1>
        `
})

export class DogFormComponent {}